{
  "name": "ojt-monorepo",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces"
  },
  "dependencies": {
    "hono": "^4.7.2",
    "react": "^19.0.0",
    "stream-chat": "^8.60.0"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1"
  }
}
